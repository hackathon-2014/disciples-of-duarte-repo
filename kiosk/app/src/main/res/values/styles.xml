<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/Widget.Hyper.ActionBar.Regular</item>
    </style>

    <style name="Theme.Hyper" parent="android:Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/Widget.Hyper.ActionBar</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="Widget.Hyper.ActionBar" parent="android:Widget.Holo.Light.ActionBar">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:titleTextStyle">@style/TextAppearance.AB.title</item>
    </style>

    <style name="TextAppearance.AB.title" parent="android:TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Widget.Hyper.ActionBar.Regular" parent="android:Widget.Holo.Light.ActionBar">
        <item name="android:background">#FF4444</item>
        <item name="android:titleTextStyle">@style/TextAppearance.AB.title</item>
    </style>

    <style name="TextAppearance.AB.title.Regular" parent="android:TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
